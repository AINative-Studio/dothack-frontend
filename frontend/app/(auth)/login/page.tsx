import { LoginForm } from '@/components/auth/LoginForm'

export const metadata = {
  title: 'Sign In — DotHack',
  description: 'Sign in to your DotHack account powered by AINative Studio',
}

export default function LoginPage() {
  return <LoginForm />
}
